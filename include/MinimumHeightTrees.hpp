#ifndef MINIMUM_HEIGHT_TREES_HPP_
#define MINIMUM_HEIGHT_TREES_HPP_

#include <vector>
#include <utility>

using namespace std;

class MinimumHeightTrees {
public:
    vector<int> findMinHeightTrees(int n, vector<pair<int, int>> &edges);
};

#endif // MINIMUM_HEIGHT_TREES_HPP_
