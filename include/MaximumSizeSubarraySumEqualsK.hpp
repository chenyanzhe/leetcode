#ifndef MAXIMUM_SIZE_SUBARRAY_SUM_EQUALS_K_HPP_
#define MAXIMUM_SIZE_SUBARRAY_SUM_EQUALS_K_HPP_

#include <vector>

using namespace std;

class MaximumSizeSubarraySumEqualsK {
public:
    int maxSubArrayLen(vector<int> &nums, int k);
};

#endif // MAXIMUM_SIZE_SUBARRAY_SUM_EQUALS_K_HPP_