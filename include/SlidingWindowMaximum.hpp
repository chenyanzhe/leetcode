#ifndef SLIDING_WINDOW_MAXIMUM_HPP_
#define SLIDING_WINDOW_MAXIMUM_HPP_

#include <vector>

using namespace std;

class SlidingWindowMaximum {
public:
    vector<int> maxSlidingWindow(vector<int> &nums, int k);
};

#endif // SLIDING_WINDOW_MAXIMUM_HPP_