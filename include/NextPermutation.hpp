#ifndef NEXT_PERMUTATION_HPP_
#define NEXT_PERMUTATION_HPP_

#include <vector>

using namespace std;

class NextPermutation {
public:
    void nextPermutation(vector<int> &nums);
};

#endif // NEXT_PERMUTATION_HPP_
