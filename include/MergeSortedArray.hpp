#ifndef MERGE_SORTED_ARRAY_HPP_
#define MERGE_SORTED_ARRAY_HPP_

#include <vector>

using namespace std;

class MergeSortedArray {
public:
    void merge(vector<int> &nums1, int m, vector<int> &nums2, int n);
};

#endif // MERGE_SORTED_ARRAY_HPP_
