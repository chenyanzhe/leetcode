#ifndef MINIMUM_WINDOW_SUBSTRING_HPP_
#define MINIMUM_WINDOW_SUBSTRING_HPP_

#include <string>

using namespace std;

class MinimumWindowSubstring {
public:
    string minWindow(string s, string t);
};

#endif // MINIMUM_WINDOW_SUBSTRING_HPP_
