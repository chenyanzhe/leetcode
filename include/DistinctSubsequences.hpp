#ifndef DISTINCT_SUBSEQUENCES_HPP_
#define DISTINCT_SUBSEQUENCES_HPP_

#include <string>

using namespace std;

class DistinctSubsequences {
public:
    int numDistinct(string s, string t);
};

#endif // DISTINCT_SUBSEQUENCES_HPP_
