#ifndef PARTITION_EQUAL_SUBSET_SUM_HPP_
#define PARTITION_EQUAL_SUBSET_SUM_HPP_

#include <vector>

using namespace std;

class PartitionEqualSubsetSum {
public:
    bool canPartition(vector<int> &nums);
};

#endif // PARTITION_EQUAL_SUBSET_SUM_HPP_